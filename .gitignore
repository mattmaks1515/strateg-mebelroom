# Секреты — НИКОГДА в гит
.env

# Локальный корень HTTPS-перехватчика (антивирус) — только для локальной разработки
corp_ca.pem

# Память диалогов пользователей (личная переписка) — не в гит
logs/dialogs/

# Рантайм-состояние (не секреты, но и не нужно в гите)
logs/digest_state.json
logs/state.json
logs/overrides.md

# Локальные настройки Claude Code
.claude/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Временный локальный помощник ввода секретов
_set_secrets.py

# Прочее
.DS_Store
